# Root env reference — copy values to backend/.env and frontend/.env

# Backend (backend/.env)
# DATABASE_URL="mysql://user:pass@host:3306/shivshakti_technology"
# JWT_SECRET="use-a-long-random-secret-in-production"
# PORT=5000
# NODE_ENV=production
# CLIENT_URL="https://yourdomain.com"

# Frontend (frontend/.env) — only if API is on a different domain
# VITE_API_URL="https://api.yourdomain.com"
